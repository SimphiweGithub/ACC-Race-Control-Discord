
netbeans.license=HEADER

# JDK 25 + Npcap on Windows: disable daemon so Gradle runs entirely in the
# wrapper JVM — no loopback socket, no WEPollSelectorProvider/PipeImpl crash.
org.gradle.daemon=false
